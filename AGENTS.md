# astrbot_plugin_hzcubing 协作说明

hzcubing 社群功能插件，主要覆盖社群成绩查询、成绩录入、排行榜、QQ 绑定、整活项目创建、CTO 打乱、俊改图片和「赛赛」成绩查询等功能。

## 初步印象

- 插件入口是 `main.py`，主要负责 AstrBot 命令注册和把请求分发到具体 handler。
- 命令逻辑集中在 `commands/`，外部或业务服务在 `services/` 与 `integrations/`，通用辅助函数在 `utils/`。
- `integrations/caicai/` 是赛赛相关集成，涉及外部平台调用，改动时要特别注意 API 失败、超时和资源关闭。
- 当前版本 `v1.0.3` 已经做过目录整理，顶层保持轻量是这个插件的重要维护方向。

## 改动原则

1. 新增命令优先放入 `commands/`，并在 `main.py` 中只做注册和分发，避免入口文件继续变胖。
2. 访问 hzcubing 或赛赛等外部服务时，保持异步友好，补齐异常处理和用户可读提示。
3. 与成绩、绑定、排行榜相关的逻辑要优先保证数据准确性；格式美化应放在正确性之后。
4. 如果新增或调整命令，同步更新 `README.md` 的命令表和 `metadata.yaml` 版本说明。
5. 图片和静态资源放在明确资源目录中，不要把临时生成文件留在插件源码目录。